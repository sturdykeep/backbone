name: example
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
   sdk: '>=2.18.4 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flame: 1.6.0
  backbone:
    git:
      url: https://github.com/sturdykeep/backbone.git
      ref: main
  perfmon_logger:
    git:
      url: https://github.com/sturdykeep/backbone_dev_tooling.git
      path: perfmon_logger/
      ref: main

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
   - assets/images/